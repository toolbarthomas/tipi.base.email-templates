{% if href is not empty and title is not empty %}
<table width="100%" border="0" cellpadding="0" cellspacing="0" class="button">
    <tbody>
        <tr>
            <td align="center" valign="middle">
                <a href="{{ href }}" target="_blank">{{ title }}</a>
            </td>
        </tr>
    </tbody>
</table>
{% endif %}